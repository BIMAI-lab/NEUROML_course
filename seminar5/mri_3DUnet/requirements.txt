# Requirements for: MRI segmentation with 3D U-Net (seminar part 3)
#
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt
#
# Needs `git` on PATH (see surface-distance at the bottom).
# A CUDA-capable GPU is strongly recommended; CPU training is impractical.

jupyterlab==4.3.4
ipykernel==6.29.5

# --- Core ---
torch>=2.2
torchvision>=0.17
numpy<2.0
pandas==2.2.2
scipy>=1.10
requests==2.32.4

# --- Medical imaging: I/O, transforms, patch sampling ---
torchio>=0.19
nibabel>=5.0
nilearn>=0.10
SimpleITK>=2.3

# --- U-Net architecture (fepegar/unet) ---
unet>=0.7.7

# --- Metrics, plotting, progress ---
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65

# --- Data download + spreadsheet reader ---
gdown==5.2.0
openpyxl>=3.1

# --- Surface-distance metrics (DSC / ASD) ---
# Used by calculate_metrics() / validate_dsc_asd(). Not on PyPI under this
# name, so it installs from GitHub. The original notebook called these
# functions but never imported or installed them.
git+https://github.com/google-deepmind/surface-distance.git
